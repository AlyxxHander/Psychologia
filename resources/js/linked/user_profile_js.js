// Show profile menu
$(document).on('mouseenter', '#profile-wrapper', function () {
  $('#profile-menu').show();
});

// Hide profile menu
$(document).on('mouseleave', '#profile-wrapper', function () {
  $('#profile-menu').hide();
});